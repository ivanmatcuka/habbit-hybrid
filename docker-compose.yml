services:
  android_builder:
    platform: linux/x86_64
    build:
      context: .
      dockerfile: Dockerfile.android
    container_name: android_builder
    volumes:
      - ./tmp:/usr/src/app/android/app/build/outputs/bundle
    ports:
      - 4174:4174
    restart: unless-stopped
