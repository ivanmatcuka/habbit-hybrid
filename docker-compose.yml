services:
  habits_hybrid:
    platform: linux/x86_64
    build:
      context: .
      dockerfile: ./scripts/development/Dockerfile.web
    container_name: habits_hybrid
    ports:
      - 4174:4174
    restart: unless-stopped
